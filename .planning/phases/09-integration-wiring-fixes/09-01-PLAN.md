---
phase: 09-integration-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/compliance.py
  - app/api/communication.py
  - app/query/prompt.py
autonomous: true

must_haves:
  truths:
    - "Frontend apiFetch('/compliance/submit/{id}') hits the compliance router (no 404)"
    - "Frontend apiFetch('/communication/confirm/{id}') hits the communication router (no 404)"
    - "LLM prompt documents expenses.attribution as 'jay', 'minnie', 'shared' — matching actual database values"
    - "Airbnb pre-arrival scheduler sends operator email, status stays 'pending' until operator confirms"
  artifacts:
    - path: "app/api/compliance.py"
      provides: "Compliance router with /api prefix"
      contains: 'prefix="/api/compliance"'
    - path: "app/api/communication.py"
      provides: "Communication router with /api prefix"
      contains: 'prefix="/api/communication"'
    - path: "app/query/prompt.py"
      provides: "Schema-aware prompt with correct attribution values"
      contains: "'jay', 'minnie', 'shared'"
  key_links:
    - from: "frontend/src/components/actions/ActionItem.tsx"
      to: "app/api/compliance.py"
      via: "apiFetch('/compliance/submit/{id}') -> prepends /api -> hits /api/compliance/submit/{id}"
      pattern: "apiFetch.*compliance"
    - from: "frontend/src/components/actions/ActionItem.tsx"
      to: "app/api/communication.py"
      via: "apiFetch('/communication/confirm/{id}') -> prepends /api -> hits /api/communication/confirm/{id}"
      pattern: "apiFetch.*communication"
---

<objective>
Fix three wiring issues: add `/api` prefix to compliance and communication routers so Actions tab buttons stop 404-ing, correct the LLM prompt's attribution values so property-specific expense queries generate valid WHERE clauses, and verify the Airbnb pre-arrival notification already works correctly.

Purpose: Action buttons in the dashboard are completely non-functional due to router prefix mismatch, and the LLM generates incorrect SQL for attribution-filtered expense queries. These are the highest-priority gaps from the v1 audit.
Output: Three one-line file edits plus a documented verification of Airbnb pre-arrival behavior.
</objective>

<execution_context>
@/Users/tunderhill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tunderhill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-integration-wiring-fixes/09-RESEARCH.md
@app/api/compliance.py
@app/api/communication.py
@app/query/prompt.py
@app/communication/messenger.py
@frontend/src/components/actions/ActionItem.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix router prefixes for compliance and communication</name>
  <files>app/api/compliance.py, app/api/communication.py</files>
  <action>
  Two one-line changes:

  1. In `app/api/compliance.py` line 25, change:
     ```python
     router = APIRouter(prefix="/compliance", tags=["compliance"])
     ```
     to:
     ```python
     router = APIRouter(prefix="/api/compliance", tags=["compliance"])
     ```

  2. In `app/api/communication.py` line 20, change:
     ```python
     router = APIRouter(prefix="/communication", tags=["communication"])
     ```
     to:
     ```python
     router = APIRouter(prefix="/api/communication", tags=["communication"])
     ```

  Also update the module docstrings at the top of each file to reflect the new URL paths (change `/compliance/` to `/api/compliance/` and `/communication/` to `/api/communication/` in the endpoint doc comments).

  Do NOT change the ingestion router prefix — ingestion endpoints use FormData fetch, not apiFetch. Changing ingestion would break file uploads.
  </action>
  <verify>Run `cd /Users/tunderhill/development/airbnb-tools && grep -n 'prefix=' app/api/compliance.py app/api/communication.py` — both must show `prefix="/api/compliance"` and `prefix="/api/communication"` respectively. Then verify all routers are consistent: `grep -n 'prefix=' app/api/*.py` — compliance, communication, accounting, dashboard, reports, query should all have `/api/` prefix.</verify>
  <done>Compliance router prefix is `/api/compliance`. Communication router prefix is `/api/communication`. Frontend apiFetch calls will now resolve to working server endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Fix LLM attribution values and verify Airbnb pre-arrival</name>
  <files>app/query/prompt.py</files>
  <action>
  1. In `app/query/prompt.py`, find line 81 in the SYSTEM_PROMPT string:
     ```
     attribution  VARCHAR(32)             -- 'property', 'shared', 'personal'
     ```
     Change it to:
     ```
     attribution  VARCHAR(32)             -- 'jay', 'minnie', 'shared'
     ```
     These are the actual valid values enforced by `app/accounting/expenses.py` (`_VALID_ATTRIBUTIONS = frozenset({"jay", "minnie", "shared"})`).

     Do NOT modify any other part of the SYSTEM_PROMPT — the example queries are correct as-is (they filter by `p.slug`, not `e.attribution`).

  2. Verify Gap 2 (Airbnb pre-arrival) is already fixed by reading `app/communication/messenger.py` `send_pre_arrival_message()`. Confirm that:
     - For `booking.platform == "airbnb"` (line 179), the function calls `send_operator_notification_with_retry()` (line 185)
     - It sets `comm_log.operator_notified_at` (line 198) but does NOT set `comm_log.status = "sent"` or `comm_log.sent_at`
     - Status stays at its original value (pending) until operator confirms via API
     Document this verification in the SUMMARY as "Gap 2 confirmed already fixed in Phase 6 — no code change needed."
  </action>
  <verify>Run `cd /Users/tunderhill/development/airbnb-tools && uv run python -c "from app.query.prompt import SYSTEM_PROMPT; assert \"'jay', 'minnie', 'shared'\" in SYSTEM_PROMPT; assert \"'property', 'shared', 'personal'\" not in SYSTEM_PROMPT; print('Attribution values correct')"` — should print "Attribution values correct".</verify>
  <done>SYSTEM_PROMPT documents attribution as 'jay', 'minnie', 'shared'. Airbnb pre-arrival behavior verified as correct (operator notification sent, status stays pending).</done>
</task>

</tasks>

<verification>
1. `grep 'prefix=' app/api/compliance.py` shows `/api/compliance`
2. `grep 'prefix=' app/api/communication.py` shows `/api/communication`
3. `grep 'prefix=' app/api/*.py` shows all non-ingestion routers use `/api/` prefix
4. `uv run python -c "from app.query.prompt import SYSTEM_PROMPT; assert \"'jay', 'minnie', 'shared'\" in SYSTEM_PROMPT"` passes
5. `app/communication/messenger.py` send_pre_arrival_message() for Airbnb sets operator_notified_at without changing status to 'sent'
</verification>

<success_criteria>
- Compliance and communication router prefixes include `/api/` — matching frontend apiFetch convention
- LLM prompt attribution values match actual database constraint ('jay', 'minnie', 'shared')
- Airbnb pre-arrival gap documented as already-satisfied (no code change)
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-wiring-fixes/09-01-SUMMARY.md`
</output>
