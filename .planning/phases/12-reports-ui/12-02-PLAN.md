---
phase: 12-reports-ui
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/src/components/reports/BalanceSheetTab.tsx
autonomous: true

must_haves:
  truths:
    - "User can select a date preset (This Month, This Quarter, YTD, Last Year) or custom date, click Generate, and see a balance sheet with Assets, Liabilities, and Equity sections"
    - "Each section is a collapsible card showing account names and balances with subtotal in header"
    - "Zero-balance accounts display em-dash instead of $0.00"
    - "Negative amounts show in red with parentheses"
    - "Total Liabilities & Equity row appears at bottom with darker shading"
    - "Before Generate is clicked, a prompt message tells user to select a date"
  artifacts:
    - path: "frontend/src/components/reports/BalanceSheetTab.tsx"
      provides: "Complete balance sheet viewer with filters, sections, and formatting"
      exports: ["BalanceSheetTab"]
  key_links:
    - from: "frontend/src/components/reports/BalanceSheetTab.tsx"
      to: "frontend/src/hooks/useReports.ts"
      via: "useBalanceSheet() hook"
      pattern: "useBalanceSheet"
    - from: "frontend/src/components/reports/BalanceSheetTab.tsx"
      to: "frontend/src/components/reports/ReportFilters.tsx"
      via: "ReportFilters mode='snapshot'"
      pattern: "ReportFilters.*mode.*snapshot"
    - from: "frontend/src/components/reports/BalanceSheetTab.tsx"
      to: "frontend/src/components/reports/ReportSection.tsx"
      via: "ReportSection for Assets/Liabilities/Equity"
      pattern: "ReportSection"
---

<objective>
Implement the Balance Sheet tab — the simplest of the three reports (single date, no monthly drill-down, three fixed sections).

Purpose: Delivers a working financial report viewer that validates the foundation from Plan 01 (ReportFilters in snapshot mode, ReportSection, useBalanceSheet hook) before the more complex P&L and Income Statement are built.
Output: BalanceSheetTab.tsx replaces the stub from Plan 01 with a full interactive viewer.
</objective>

<execution_context>
@/Users/tunderhill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tunderhill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-reports-ui/12-CONTEXT.md
@.planning/phases/12-reports-ui/12-RESEARCH.md
@.planning/phases/12-reports-ui/12-01-SUMMARY.md
@frontend/src/api/reports.ts
@frontend/src/hooks/useReports.ts
@frontend/src/components/reports/ReportSection.tsx
@frontend/src/components/reports/ReportFilters.tsx
@frontend/src/components/reports/BalanceSheetTab.tsx
@frontend/src/components/shared/EmptyState.tsx
@frontend/src/components/shared/ErrorAlert.tsx
@frontend/src/components/ui/skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Balance Sheet viewer</name>
  <files>frontend/src/components/reports/BalanceSheetTab.tsx</files>
  <action>
OVERWRITE the stub `frontend/src/components/reports/BalanceSheetTab.tsx` with the full Balance Sheet viewer.

**Imports:**
- `useBalanceSheet` from `@/hooks/useReports`
- `ReportFilters` from `./ReportFilters`
- `ReportSection` from `./ReportSection`
- `EmptyState` from `@/components/shared/EmptyState`
- `ErrorAlert` from `@/components/shared/ErrorAlert`
- `Skeleton` from `@/components/ui/skeleton`

**Number formatting utility** (define at top of file or in a local helper):
```typescript
function formatAmount(value: string): { display: string; isNegative: boolean; isZero: boolean } {
  const num = parseFloat(value)
  if (isNaN(num) || num === 0) return { display: '\u2014', isNegative: false, isZero: true }
  const formatted = Math.abs(num).toLocaleString('en-US', { style: 'currency', currency: 'USD' })
  return {
    display: num < 0 ? `(${formatted})` : formatted,
    isNegative: num < 0,
    isZero: false,
  }
}
```
- Zero values: em-dash (—) per CONTEXT decision
- Negative values: red text with parentheses (Claude's discretion from CONTEXT — this is the non-accountant-friendly pattern)
- Positive values: standard currency format

**AmountCell component** (local, not exported):
```tsx
function AmountCell({ value, className = '' }: { value: string; className?: string }) {
  const { display, isNegative } = formatAmount(value)
  return (
    <span className={`tabular-nums ${isNegative ? 'text-red-600 dark:text-red-400' : ''} ${className}`}>
      {display}
    </span>
  )
}
```

**Component structure:**

1. Call `useBalanceSheet()` to get `{ data, isFetching, isError, error, refetch, generate, hasGenerated }`

2. Render `ReportFilters` with `mode="snapshot"`, `onGenerate` that calls `generate({ as_of: params.as_of })`, and `isFetching`.

3. **State rendering logic:**
   - If `!hasGenerated`: Show prompt message — "Select a date and click Generate to view the balance sheet." (centered, muted text, py-16, similar to EmptyState styling)
   - If `isFetching` and no data: Show 3 Skeleton blocks (one per section, ~h-32 each)
   - If `isError`: Show `ErrorAlert` with `error.message` and `onRetry={refetch}`
   - If `data` exists but all totals are "0.00" or "0": Show `EmptyState` with title="No balance sheet data" and description="No account activity found for this date."
   - If `data` exists with content: Show report sections

4. **Report header** (when data exists):
   - Small muted text showing "As of {formatted date}" — format the as_of date as "March 2, 2026" style

5. **Three ReportSections** (standard balance sheet order per research recommendation):

   **Assets section** (`title="Assets"`, `total={formatAmount(data.assets.total).display}`):
   - Table with two columns: Account Name (left-aligned) | Balance (right-aligned)
   - Each account row: `data.assets.accounts.map(a => ...)` showing `a.name` and `AmountCell` for `a.balance`
   - Subtotal row with `bg-muted/30 font-medium` background: "Total Assets" | AmountCell for `data.assets.total`

   **Liabilities section** (`title="Liabilities"`, `total={formatAmount(data.liabilities.total).display}`):
   - Same table structure as Assets
   - Each account: `data.liabilities.accounts.map(a => ...)`
   - Subtotal row: "Total Liabilities" | AmountCell

   **Equity section** (`title="Equity"`, `total={formatAmount(data.equity.total).display}`):
   - Same table structure
   - Each account: `data.equity.accounts.map(a => ...)`
   - Subtotal row: "Total Equity" | AmountCell

6. **Grand total row** (outside sections, in its own Card or styled div):
   - `bg-muted/60 font-semibold border-t-2` styling
   - "Total Liabilities & Equity" | AmountCell for `data.total_liabilities_and_equity`
   - If `data.assets.total !== data.total_liabilities_and_equity`: show a small amber warning "Balance sheet does not balance" (this should never happen in a working system, but provides transparency per research)

**Table styling:**
- Use `<table className="w-full text-sm">` with `<tbody>` for each section's content
- Account name cells: `px-3 py-1.5 text-left`
- Amount cells: `px-3 py-1.5 text-right`
- All amount cells have `tabular-nums` for alignment
  </action>
  <verify>
1. `cd frontend && npx tsc --noEmit` — no type errors
2. `cd frontend && npm run build` — build succeeds
3. Manual: navigate to Reports > Balance Sheet, select YTD preset, click Generate — balance sheet renders with three collapsible sections
  </verify>
  <done>Balance sheet viewer shows Assets, Liabilities, Equity sections with collapsible cards, formatted amounts, em-dashes for zeros, red parentheses for negatives, and grand total row. Prompt state shown before first Generate.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Balance Sheet integration</name>
  <files>frontend/src/components/reports/BalanceSheetTab.tsx</files>
  <action>
Run the following verification steps:

1. `cd frontend && npx tsc --noEmit` — ensure zero type errors across entire frontend
2. `cd frontend && npm run build` — ensure production build succeeds
3. Review that BalanceSheetTab correctly:
   - Imports useBalanceSheet from @/hooks/useReports
   - Imports ReportFilters and ReportSection from sibling files
   - Uses mode="snapshot" on ReportFilters
   - Shows prompt state when hasGenerated is false
   - Shows loading skeletons when isFetching
   - Shows error state with retry
   - Renders 3 ReportSections for Assets, Liabilities, Equity
   - Shows grand total row

If any issues found during verification, fix them in BalanceSheetTab.tsx.
  </action>
  <verify>`cd frontend && npm run build` passes with zero errors.</verify>
  <done>Balance Sheet tab compiles, builds, and correctly wires all Plan 01 foundation components.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` — zero errors
2. Navigate to Reports > Balance Sheet — sub-tab renders
3. Click "YTD" preset then "Generate" — balance sheet data loads (or shows empty state if no data in DB)
4. Collapse/expand sections — animation works
5. Check amount formatting: zeros show "—", layout is aligned with tabular-nums
</verification>

<success_criteria>
- BalanceSheetTab replaces stub with full interactive viewer
- ReportFilters mode="snapshot" computes as_of date correctly from presets
- Three ReportSections (Assets, Liabilities, Equity) render collapsible cards
- Number formatting: red parentheses for negative, em-dash for zero, currency for positive
- Grand total row with darker shading at bottom
- Prompt state before Generate, loading skeletons during fetch, error with retry
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-reports-ui/12-02-SUMMARY.md`
</output>
