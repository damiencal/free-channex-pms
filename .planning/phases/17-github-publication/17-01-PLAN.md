---
phase: 17-github-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "GitHub repository captainarcher/roost exists (private)"
    - "Full git history is pushed to GitHub"
    - "Repository has description, topics, issues enabled, discussions enabled, wiki disabled"
    - "All code, docs, and LICENSE file are present on GitHub"
  artifacts:
    - path: "captainarcher/roost (GitHub)"
      provides: "Private GitHub repository with full history"
  key_links:
    - from: "local git repo"
      to: "github.com/captainarcher/roost"
      via: "origin remote"
      pattern: "git remote -v.*captainarcher/roost"
---

<objective>
Create the GitHub repository captainarcher/roost as private, push the full git history, and configure all repository metadata (description, topics, issues, discussions, wiki).

Purpose: Establish the repository on GitHub in private mode so the user can review rendering before going public.
Output: A fully configured private GitHub repository with all code, docs, and metadata ready for public flip.
</objective>

<execution_context>
@/Users/tunderhill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tunderhill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-github-publication/17-CONTEXT.md
@.planning/phases/17-github-publication/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create private repository and push full history</name>
  <files>No local files modified — remote repository creation</files>
  <action>
    Create the GitHub repository using `gh repo create` with the local repo as source:

    ```bash
    gh repo create captainarcher/roost \
      --private \
      --description "Automated vacation rental management — booking to accounting with zero manual intervention" \
      --disable-wiki \
      --source=. \
      --remote=origin \
      --push
    ```

    IMPORTANT: There is no existing origin remote — `--remote=origin` will work cleanly.
    IMPORTANT: Do NOT use `--enable-discussions` on `gh repo create` — that flag does not exist there. Discussions are enabled in Task 2.
    IMPORTANT: Push full history (not shallow) — the git history was scrubbed of PII in Phase 14 and shows project evolution.

    After the push completes, verify the remote is set and all branches/tags are pushed:
    ```bash
    git remote -v
    git log --oneline -3 origin/main
    ```
  </action>
  <verify>
    Run: `gh repo view captainarcher/roost --json name,visibility,description`
    Expected: name=roost, visibility=PRIVATE, description matches.
    Run: `git remote -v` shows origin pointing to captainarcher/roost.
  </verify>
  <done>
    GitHub repository captainarcher/roost exists as private. Full git history is pushed. Origin remote is configured locally.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure repository metadata (topics, discussions, issues)</name>
  <files>No local files modified — remote repository configuration</files>
  <action>
    Configure topics and features using `gh repo edit`:

    ```bash
    gh repo edit captainarcher/roost \
      --add-topic vacation-rental \
      --add-topic property-management \
      --add-topic self-hosted \
      --add-topic docker \
      --add-topic fastapi \
      --add-topic react \
      --add-topic airbnb \
      --add-topic python \
      --add-topic typescript \
      --add-topic automation \
      --add-topic accounting \
      --add-topic open-source \
      --enable-issues \
      --enable-discussions
    ```

    Topics rationale: 6 locked from roadmap (vacation-rental, property-management, self-hosted, docker, fastapi, react) + 6 additions for discoverability (airbnb, python, typescript, automation, accounting, open-source). Total: 12 of 20 max.

    Wiki was already disabled via `--disable-wiki` in Task 1. Issues are enabled by default but explicitly set here for clarity.

    After configuration, verify all metadata:
    ```bash
    gh repo view captainarcher/roost --json topics,hasIssuesEnabled,hasWikiEnabled,hasDiscussionsEnabled
    ```
  </action>
  <verify>
    Run: `gh repo view captainarcher/roost --json topics,hasIssuesEnabled,hasWikiEnabled,hasDiscussionsEnabled`
    Expected: 12 topics listed, issues=true, wiki=false, discussions=true.
  </verify>
  <done>
    Repository has all 12 topics, issues enabled, discussions enabled, wiki disabled. All metadata is configured and ready for user review.
  </done>
</task>

</tasks>

<verification>
1. `gh repo view captainarcher/roost --json name,visibility,description,topics,hasIssuesEnabled,hasWikiEnabled,hasDiscussionsEnabled` — all fields match expected values
2. `git remote -v` — origin points to captainarcher/roost
3. `gh api repos/captainarcher/roost --jq '.license'` — returns apache-2.0 (GitHub auto-detects from LICENSE file)
4. README.md, LICENSE, NOTICE, CONTRIBUTING.md, CHANGELOG.md all visible via `gh api repos/captainarcher/roost/contents/ --jq '.[].name'`
</verification>

<success_criteria>
- Private repository captainarcher/roost exists on GitHub
- Full git history pushed (not just latest commit)
- Description set: "Automated vacation rental management — booking to accounting with zero manual intervention"
- 12 topics configured
- Issues enabled, Discussions enabled, Wiki disabled
- LICENSE file detected by GitHub as Apache 2.0
</success_criteria>

<output>
After completion, create `.planning/phases/17-github-publication/17-01-SUMMARY.md`
</output>
