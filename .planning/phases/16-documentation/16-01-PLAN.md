---
phase: 16-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true

must_haves:
  truths:
    - "A visitor landing on the repo understands what Roost does within 30 seconds"
    - "Feature list covers all major capabilities (ingestion, accounting, compliance, communication, dashboard, NL query)"
    - "Quick start section provides copy-paste commands that get a user from zero to running"
    - "CHANGELOG.md has a v1.0 entry listing all shipped capabilities in keepachangelog format"
  artifacts:
    - path: "README.md"
      provides: "Project overview, features, screenshots, quick start, config reference, badges"
      contains: "## Features"
    - path: "CHANGELOG.md"
      provides: "keepachangelog v1.1.0 formatted changelog with v1.0 release entry"
      contains: "## [1.0.0]"
  key_links:
    - from: "README.md"
      to: "docs/architecture.md"
      via: "markdown link"
      pattern: "\\[.*\\]\\(docs/architecture\\.md\\)"
    - from: "README.md"
      to: "docs/deployment.md"
      via: "markdown link"
      pattern: "\\[.*\\]\\(docs/deployment\\.md\\)"
    - from: "README.md"
      to: "docs/api.md"
      via: "markdown link"
      pattern: "\\[.*\\]\\(docs/api\\.md\\)"
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "markdown link"
      pattern: "\\[.*\\]\\(CONTRIBUTING\\.md\\)"
---

<objective>
Create the README.md and CHANGELOG.md — the two public-facing root documents that establish first impressions and project history.

Purpose: README.md is the single most important file for open source adoption. CHANGELOG.md establishes project maturity.
Output: Complete README.md (replaces existing stub) and new CHANGELOG.md at repo root.
</objective>

<execution_context>
@/Users/tunderhill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tunderhill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-documentation/16-CONTEXT.md
@.planning/phases/16-documentation/16-RESEARCH.md
@README.md
@docker-compose.yml
@.env.example
@config/base.example.yaml
@config/config.example.yaml
@manage.py
@app/main.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md</name>
  <files>README.md</files>
  <action>
Replace the existing README.md stub with a comprehensive project README following the Fastify/Caddy-inspired structure specified in CONTEXT.md and RESEARCH.md.

**Structure (in this order):**

1. **Title + tagline**: `# Roost` followed by a one-line tagline (e.g., "Self-hosted vacation rental operations platform").

2. **Badge row**: Four shields.io badges on one line (per RESEARCH.md):
   - License: Apache 2.0 — `https://img.shields.io/github/license/captainarcher/roost`
   - Python 3.12+ — `https://img.shields.io/badge/python-3.12+-blue`
   - Docker — `https://img.shields.io/badge/docker--compose-ready-blue?logo=docker`
   - FastAPI — `https://img.shields.io/badge/FastAPI-0.115+-009688?logo=fastapi`

3. **Description**: 2-3 sentences explaining what Roost does. Emphasize: automated end-to-end rental operations, multi-platform support (Airbnb, VRBO, RVshare), self-hosted with Docker, zero manual intervention after config.

4. **Features**: Bulleted list covering all major capabilities:
   - Multi-platform booking ingestion (Airbnb CSV, VRBO CSV, RVshare manual entry, Mercury bank CSV)
   - Double-entry accounting with automated revenue recognition
   - Financial reports (P&L, balance sheet, income statement)
   - Resort compliance automation (PDF form filling, email submission)
   - Guest communication orchestration (welcome messages, pre-arrival messages)
   - Interactive dashboard with real-time metrics
   - Natural language query interface (via local Ollama — optional)
   - Config-driven architecture (YAML — no hardcoded property data)
   - Docker Compose deployment (single `docker compose up`)

5. **Screenshots**: Placeholder section with three entries (dashboard, booking detail, accounting). Use alt text like `![Dashboard — key metrics and occupancy overview](docs/screenshots/dashboard.png)`. Add a note: "Screenshots coming soon."

6. **Quick Start**: Copy-paste commands section (based on existing README but improved):
   - Prerequisites: Docker and Docker Compose. Ollama optional.
   - Step 1: Clone + cd
   - Step 2: `cp .env.example .env` — edit POSTGRES_PASSWORD + update DATABASE_URL to match
   - Step 3: Copy config examples — `cp config/base.example.yaml config/base.yaml` and `cp config/config.example.yaml config/{your-property-slug}.yaml` — edit with real values
   - Step 4: `docker compose up`
   - Step 5: Verify with `curl http://localhost:8000/health`
   - Link to full deployment guide: `docs/deployment.md`

7. **Documentation**: Table or list linking to:
   - `docs/deployment.md` — Full deployment guide
   - `docs/architecture.md` — Architecture overview
   - `docs/api.md` — API reference
   - `CONTRIBUTING.md` — Contributing guide
   - `CHANGELOG.md` — Changelog

8. **Configuration**: Brief table of config files (same structure as existing README but add .env details). Link to deployment guide for full walkthrough.

9. **CLI**: `manage.py` commands: `setup` (interactive wizard), `list-properties` (list configured properties).

10. **Tech Stack**: Brief list — Python 3.12 / FastAPI / SQLAlchemy 2.0 / PostgreSQL 16 / React / Vite / Ollama / Docker.

11. **License**: "Apache 2.0 — see [LICENSE](LICENSE)."

**Tone**: Professional and concise. No marketing fluff. Clean, minimal — let the project speak for itself. Do NOT use emojis.
  </action>
  <verify>
- `cat README.md | head -5` shows `# Roost` title
- `grep -c '## ' README.md` returns 8+ (one per major section)
- `grep 'shields.io' README.md | wc -l` returns 4 (four badges)
- `grep 'docs/deployment.md' README.md` returns at least one match (link to deployment guide)
- `grep 'docs/architecture.md' README.md` returns at least one match
- `grep 'docs/api.md' README.md` returns at least one match
- `grep 'CONTRIBUTING.md' README.md` returns at least one match
- `grep 'CHANGELOG.md' README.md` returns at least one match
- `grep 'docker compose up' README.md` returns at least one match
- `grep -i 'screenshots coming soon\|placeholder' README.md` returns at least one match (screenshots are placeholders)
  </verify>
  <done>README.md is a complete, professional project overview with badges, features, screenshots placeholder, quick start, doc links, config reference, CLI, tech stack, and license sections. A visitor understands what Roost does within 30 seconds.</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <action>
Create CHANGELOG.md at repo root following keepachangelog v1.1.0 format exactly (per CONTEXT.md and RESEARCH.md).

**Structure:**

```
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [1.0.0] - 2026-03-02

### Added
[v1.0 feature list]

[1.0.0]: https://github.com/captainarcher/roost/releases/tag/v1.0.0
[Unreleased]: https://github.com/captainarcher/roost/compare/v1.0.0...HEAD
```

**v1.0 Added section** — list the major capabilities that shipped (from PROJECT.md validated requirements):
- Multi-platform booking ingestion from Airbnb, VRBO, RVshare, and Mercury bank exports
- Double-entry accounting engine with chart of accounts, journal entries, and automated revenue recognition
- Financial reporting: profit & loss, balance sheet, and income statement with period filtering
- Resort compliance automation: PDF form filling, email submission, urgency tracking, and operator approval workflow
- Guest communication orchestration: welcome messages and pre-arrival messages with platform-aware delivery (Airbnb native, VRBO/RVshare operator-assisted)
- Interactive React dashboard with financial metrics, occupancy charts, booking calendar, and action items
- Natural language query interface powered by local Ollama (text-to-SQL with streaming narrative response)
- Config-driven architecture: YAML property configs, Jinja2 message templates, JSON PDF field mappings
- Docker Compose deployment with PostgreSQL and automatic database migrations
- CLI management tool (`manage.py`) for interactive property setup

Use the date 2026-03-02 (v1.0 ship date from ROADMAP.md).
  </action>
  <verify>
- `test -f CHANGELOG.md` exits 0 (file exists)
- `grep 'Keep a Changelog' CHANGELOG.md` returns a match (format attribution)
- `grep '## \[1.0.0\] - 2026-03-02' CHANGELOG.md` returns a match (version + date)
- `grep '### Added' CHANGELOG.md` returns a match (Added section)
- `grep '\[Unreleased\]' CHANGELOG.md` returns a match (Unreleased section)
- `grep 'captainarcher/roost' CHANGELOG.md` returns a match (GitHub link references)
  </verify>
  <done>CHANGELOG.md exists with a complete v1.0 release entry in keepachangelog format, listing all major shipped capabilities, with proper GitHub release links.</done>
</task>

</tasks>

<verification>
- README.md replaces the existing stub with comprehensive content
- CHANGELOG.md is a new file at repo root
- Both files use consistent project name "Roost"
- README links to docs/ files and other root docs (CONTRIBUTING, CHANGELOG)
- No emojis in either file
- Professional, concise tone throughout
</verification>

<success_criteria>
1. README.md has badges, features, screenshots placeholder, quick start, doc links, config table, CLI, tech stack, and license
2. CHANGELOG.md follows keepachangelog v1.1.0 format with v1.0 entry dated 2026-03-02
3. A new visitor to the repo can understand what Roost does, how to set it up, and where to find deeper documentation
</success_criteria>

<output>
After completion, create `.planning/phases/16-documentation/16-01-SUMMARY.md`
</output>
