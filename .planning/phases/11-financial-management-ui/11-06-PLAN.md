---
phase: 11-financial-management-ui
plan: 06
type: execute
wave: 3
depends_on: ["11-03", "11-04", "11-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Finance tab is visible and navigable in the dashboard"
    - "Transaction categorization works end-to-end: view, filter, assign category, bulk assign"
    - "Expense entry creates a record visible in accounting data"
    - "Loan payment entry with P&I split updates loan balance"
    - "Reconciliation panel shows matches and supports confirm/reject workflow"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Finance tab across all three sub-tabs.

Purpose: Confirms all four success criteria are met with real user interaction before marking Phase 11 complete.

Output: Verified Finance tab or list of issues to address.
</objective>

<execution_context>
@/Users/tunderhill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tunderhill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-financial-management-ui/11-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build frontend and verify compilation</name>
  <files></files>
  <action>
Run the full frontend build to verify everything compiles and bundles correctly:

```bash
cd /Users/tunderhill/development/airbnb-tools/frontend && npm run build
```

If build fails, diagnose and fix TypeScript or bundling errors. Common issues to check:
- Import path typos
- Missing exports
- Type mismatches between hooks and components
- Circular imports

Also run TypeScript check:
```bash
npx tsc --noEmit
```

If there are any compilation errors, fix them before proceeding to human verification.
  </action>
  <verify>
`npm run build` exits with code 0 and produces `dist/` output. `npx tsc --noEmit` reports no errors.
  </verify>
  <done>Frontend builds and compiles without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Finance tab with three sub-tabs:
1. **Transactions** — Bank transaction table with category assignment (auto-save + attribution for expense categories), multi-select checkboxes with bulk toolbar, filters (category/date/amount), Load More pagination
2. **Expenses & Loans** — Togglable form for recording expenses (with attribution + property) and loan payments (with P&I split from dropdown of active loans)
3. **Reconciliation** — Split-panel layout with platform payouts (left) and bank deposits (right), color-coded pending confirmation matches, needs-review with candidate selection, confirm/reject actions, summary stats, Run Reconciliation button
  </what-built>
  <how-to-verify>
Start the development server if not running:
```
cd /Users/tunderhill/development/airbnb-tools && docker-compose up -d
cd frontend && npm run dev
```

**Test 1: Finance tab visibility**
1. Open http://localhost:5173
2. Verify "Finance" tab appears in the top navigation (between Actions and Query)
3. Verify Finance tab badge shows a count if there are uncategorized transactions or unreconciled items
4. Click Finance tab — should show three sub-tabs: Transactions, Expenses & Loans, Reconciliation

**Test 2: Transaction categorization (Success Criterion 1)**
1. In Transactions sub-tab, verify bank transactions appear in a table
2. Pick a non-expense category (e.g., "transfer") on one transaction — should save immediately with green "Saved" flash
3. Pick an expense category (e.g., "utilities") — should show attribution prompt (jay/minnie/shared) before saving
4. Select multiple transactions with checkboxes — verify bulk toolbar appears
5. Use bulk assign to categorize multiple at once
6. Test filters: switch to "Uncategorized" to see only uncategorized transactions
7. If many transactions exist, verify Load More button works

**Test 3: Expense recording (Success Criterion 2)**
1. Switch to Expenses & Loans sub-tab
2. Fill in expense form: date, amount, category, description, attribution
3. Submit — verify inline success message appears and form resets
4. Submit another expense — verify batch entry flow works smoothly

**Test 4: Loan payment (Success Criterion 3)**
1. Toggle to "Loan Payment"
2. Select a loan from dropdown — verify it shows "Name - $X remaining"
3. Enter principal, interest, payment_date, payment_ref
4. Submit — verify success message
5. Submit same payment_ref again — verify "Already recorded" warning

**Test 5: Reconciliation (Success Criterion 4)**
1. Switch to Reconciliation sub-tab
2. Verify summary stats show at top
3. Click "Run Reconciliation" — verify it processes and results appear
4. If pending confirmation matches exist: verify amber highlighting on both panels
5. Click "Confirm" on a match — verify it disappears from unreconciled view
6. If needs-review items exist: expand to see candidate bookings, pick one, confirm
7. Verify mobile responsive: resize browser to check vertical panel stacking

**Test 6: URL deep linking**
1. Navigate to http://localhost:5173/?tab=finance&ftab=reconciliation
2. Verify it opens directly to the Reconciliation sub-tab
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe specific issues found.</resume-signal>
</task>

</tasks>

<verification>
All four phase success criteria verified through manual testing.
</verification>

<success_criteria>
All tests in the human verification pass. The Finance tab is fully functional.
</success_criteria>

<output>
After completion, create `.planning/phases/11-financial-management-ui/11-06-SUMMARY.md`
</output>
