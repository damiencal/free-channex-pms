---
phase: 14-license-audit-repository-hygiene
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - config/base.yaml
  - config/config.example.yaml
  - app/config.py
  - app/compliance/confirmation.py
autonomous: true

must_haves:
  truths:
    - ".gitignore excludes .env, archive/, config/*.yaml (except config.example.yaml), db_data/, confirmations/, and *.csv"
    - "config/jay.yaml and config/minnie.yaml are no longer tracked by git"
    - "config/base.yaml contains no real names (no 'CHANGE_ME', no real person names)"
    - "config/config.example.yaml contains only placeholder values (CHANGE_ME pattern for PII fields)"
    - "app/config.py contains no hardcoded PII (resort_contact_name default is empty string, not a real name)"
    - "app/compliance/confirmation.py docstring contains no real names"
    - "No real property addresses, guest data, or financial data exist in any tracked file"
    - "Test fixture CSV files use clearly fake sample data"
  artifacts:
    - path: ".gitignore"
      provides: "Comprehensive exclusion of private data files"
      contains: "archive/"
    - path: "config/base.yaml"
      provides: "System config with no PII"
      contains: "CHANGE_ME"
    - path: "config/config.example.yaml"
      provides: "Example config with only placeholder values"
      contains: "CHANGE_ME"
    - path: "app/config.py"
      provides: "AppConfig with no hardcoded PII defaults"
      contains: 'resort_contact_name: str = ""'
    - path: "app/compliance/confirmation.py"
      provides: "Email formatting with no PII in docstrings"
  key_links: []
---

<objective>
Update .gitignore to exclude all private data, remove real config files from git tracking, and scrub PII from any remaining tracked files including Python source.

Purpose: Ensure no private data (real names, addresses, guest info, financial data) exists in any tracked file.
Output: Clean .gitignore, scrubbed config files, scrubbed Python source defaults, property-specific configs untracked.
</objective>

<execution_context>
@/Users/tunderhill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tunderhill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-license-audit-repository-hygiene/14-RESEARCH.md
@.gitignore
@config/base.yaml
@config/config.example.yaml
@config/jay.yaml
@config/minnie.yaml
@app/config.py
@app/compliance/confirmation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .gitignore and untrack private files</name>
  <files>.gitignore</files>
  <action>
    **Step 1: Update .gitignore with comprehensive exclusions**

    Add the following entries to .gitignore (keep existing entries, add new ones in organized sections):

    ```gitignore
    # Private config files (keep only the examples)
    config/*.yaml
    !config/config.example.yaml
    !config/base.example.yaml

    # Archive and confirmations (contain real booking data)
    archive/
    confirmations/

    # Data files
    *.csv
    !tests/fixtures/*.csv

    # Frontend build output
    frontend/node_modules/
    ```

    IMPORTANT: The existing `.gitignore` already has `.env`, `db_data/`, `__pycache__/`, etc. Do NOT duplicate those. Only add the missing entries.

    Remove the existing comment at the bottom of .gitignore that says `# Note: config/*.yaml files are committed` since that is no longer true.

    **Step 2: Remove tracked files that should now be ignored**

    base.yaml contains `resort_contact_name: "CHANGE_ME"` (PII) alongside system settings like ollama_url and archive_dir. The approach is: untrack base.yaml, create a scrubbed `config/base.example.yaml` template (Task 2), and keep it gitignored. Users copy base.example.yaml to base.yaml locally.

    Run:
    ```bash
    git rm --cached config/base.yaml config/jay.yaml config/minnie.yaml
    ```

    This stops tracking these files while leaving them on disk.
  </action>
  <verify>
    1. `git rm --cached config/base.yaml config/jay.yaml config/minnie.yaml` succeeds (or shows "did not match" if already untracked)
    2. `git status` shows config/base.yaml, config/jay.yaml, config/minnie.yaml as deleted from index (but still on disk)
    3. `grep "config/\*.yaml" .gitignore` shows the pattern exists
    4. `grep "archive/" .gitignore` shows archive is excluded
    5. `grep "confirmations/" .gitignore` shows confirmations is excluded
    6. `grep "\*.csv" .gitignore` shows CSVs are excluded
    7. `grep "!tests/fixtures/\*.csv" .gitignore` shows test fixtures are excepted
    8. `grep "!config/config.example.yaml" .gitignore` shows example config is excepted
    9. `grep "!config/base.example.yaml" .gitignore` shows base example is excepted
    10. `test -f config/jay.yaml` confirms file still exists on disk (just untracked)
  </verify>
  <done>
    .gitignore comprehensively excludes all private data. config/jay.yaml, config/minnie.yaml, and config/base.yaml are untracked from git but still exist on disk. Test fixture CSVs remain tracked.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scrub PII from tracked config files, Python source, and create base.example.yaml</name>
  <files>
    config/config.example.yaml
    config/base.example.yaml
    app/config.py
    app/compliance/confirmation.py
  </files>
  <action>
    **Step 1: Scrub config/config.example.yaml**

    The current config.example.yaml has one PII issue on line 14:
    ```yaml
    host_name: "CHANGE_ME"
    ```

    Change this to:
    ```yaml
    host_name: "CHANGE_ME"
    ```

    Verify all other values in config.example.yaml are generic/placeholder. Currently they look clean (my-cabin, 555-123-4567, resort@example.com, etc.) but double-check every field.

    **Step 2: Create config/base.example.yaml**

    Create a new file `config/base.example.yaml` as the template for system-wide config. Base it on the current config/base.yaml but replace all PII:

    ```yaml
    # System-wide configuration
    # Copy this file to base.yaml and update values for your setup
    # Secrets (database, API keys, SMTP credentials) go in .env, not here

    ollama_url: "http://host.docker.internal:11434"
    ollama_model: "llama3.2:latest"
    archive_dir: "./archive"
    airbnb_fee_model: "split_fee"
    airbnb_host_fee_rate: 0.03

    # Resort compliance settings
    confirmations_dir: "./confirmations"
    pdf_template_path: "pdf_mappings/sun_retreats_booking.pdf"
    pdf_mapping_path: "pdf_mappings/sun_retreats_booking.json"
    auto_submit_threshold: 3
    resort_contact_name: "CHANGE_ME"
    ```

    The only PII to replace is `resort_contact_name: "CHANGE_ME"` -> `resort_contact_name: "CHANGE_ME"`.

    **Step 3: Scrub hardcoded PII from app/config.py**

    In `app/config.py`, line 196, change the default value for `resort_contact_name`:

    FROM:
    ```python
    resort_contact_name: str = "CHANGE_ME"
    """Resort contact name used in email subject/body."""
    ```

    TO:
    ```python
    resort_contact_name: str = ""
    """Resort contact name used in email subject/body. Set in base.yaml."""
    ```

    Using an empty string (not "CHANGE_ME") because this is a Python Pydantic field default -- an empty string will cause a clear failure if not configured, and is the standard pattern for required-at-runtime config. The real value comes from base.yaml at load time.

    **Step 4: Scrub PII from app/compliance/confirmation.py docstring**

    In `app/compliance/confirmation.py`, line 88, the docstring contains a real name as an example:
    ```python
        contact_name: Resort contact's first name (e.g., "CHANGE_ME").
    ```

    Change to a generic example:
    ```python
        contact_name: Resort contact's first name (e.g., "Jane").
    ```

    Also on line 81, the default parameter uses a real name:
    ```python
    sender_name: str = "Thomas",
    ```

    This is a function default, not a config value. Change it to pull from a neutral default:
    ```python
    sender_name: str = "Host",
    ```

    And update line 89 similarly:
    ```python
        sender_name: Host/sender name for the sign-off (default: "Host").
    ```

    **Step 5: Verify test fixture data is fake**

    Check `tests/fixtures/airbnb_sample.csv`, `tests/fixtures/mercury_sample.csv`, and `tests/fixtures/vrbo_sample.csv` for any real PII. Per the research, they use fake names (Jane Sample, John Sample, Alice Johnson, Bob Martinez, Carol Smith) and fake account numbers. Confirm this and document.

    **Step 6: Scan all tracked files for remaining PII patterns**

    Run a comprehensive grep across all tracked files for known PII patterns:
    ```bash
    git ls-files | xargs grep -l "CHANGE_ME\|CHANGE_ME\|Fort Myers Beach.*Lot\|Sun Retreats Fort Myers" 2>/dev/null
    ```

    The config/jay.yaml and config/minnie.yaml contain real addresses ("CHANGE_ME_ADDRESS/170") but they are now untracked (Task 1), so they should NOT appear in this search.

    If any tracked files still contain PII, fix them. Planning files in `.planning/` are acceptable exceptions.
  </action>
  <verify>
    1. `grep "CHANGE_ME" config/config.example.yaml` returns nothing
    2. `grep "CHANGE_ME" config/config.example.yaml` returns the host_name line
    3. `test -f config/base.example.yaml` -- file exists
    4. `grep "CHANGE_ME" config/base.example.yaml` returns the resort_contact_name line
    5. `grep "CHANGE_ME" config/base.example.yaml` returns nothing
    6. `grep 'resort_contact_name: str = "CHANGE_ME"' app/config.py` returns nothing
    7. `grep 'resort_contact_name: str = ""' app/config.py` returns the line with empty-string default
    8. `grep "CHANGE_ME" app/compliance/confirmation.py` returns nothing
    9. `grep '"Thomas"' app/compliance/confirmation.py` returns nothing
    10. `git ls-files | xargs grep -l "CHANGE_ME" 2>/dev/null` returns nothing (or only .planning/ files which are acceptable)
    11. `git ls-files | xargs grep -l "CHANGE_ME" 2>/dev/null` returns nothing (or only .planning/ files)
    12. Test fixtures still contain fake names (Jane Sample, John Sample, etc.)
  </verify>
  <done>
    All tracked config files contain only placeholder values. config/config.example.yaml has CHANGE_ME for host_name. config/base.example.yaml is a clean template with CHANGE_ME for resort_contact_name. app/config.py has an empty-string default for resort_contact_name (no hardcoded "CHANGE_ME"). app/compliance/confirmation.py docstrings and defaults use generic names only. No real PII exists in any tracked non-planning file.
  </done>
</task>

</tasks>

<verification>
1. `git ls-files -- config/` shows ONLY config/config.example.yaml and config/base.example.yaml (not base.yaml, jay.yaml, or minnie.yaml)
2. `git ls-files | xargs grep -il "CHANGE_ME\|CHANGE_ME" 2>/dev/null` returns nothing outside .planning/
3. `grep 'resort_contact_name: str = ""' app/config.py` confirms empty default
4. `grep "CHANGE_ME" app/compliance/confirmation.py` returns nothing
5. .gitignore correctly excludes archive/, confirmations/, *.csv (except test fixtures), config/*.yaml (except examples)
6. All tracked files pass a PII scan for known patterns
</verification>

<success_criteria>
- .gitignore excludes all private data directories and config files
- config/jay.yaml, config/minnie.yaml, config/base.yaml are untracked
- config/config.example.yaml and config/base.example.yaml contain ONLY placeholder values
- app/config.py resort_contact_name default is empty string, not "CHANGE_ME"
- app/compliance/confirmation.py contains no real names in docstrings or defaults
- No real names, addresses, or private data exist in any tracked file (outside .planning/)
- Test fixture CSVs remain tracked and use clearly fake data
</success_criteria>

<output>
After completion, create `.planning/phases/14-license-audit-repository-hygiene/14-03-SUMMARY.md`
</output>
