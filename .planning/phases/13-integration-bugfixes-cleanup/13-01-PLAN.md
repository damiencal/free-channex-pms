---
phase: 13-integration-bugfixes-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/actions/RVshareEntryForm.tsx
  - frontend/src/components/finance/CategorySelect.tsx
  - app/accounting/revenue.py
  - frontend/src/components/reports/IncomeStatementTab.tsx
autonomous: true

must_haves:
  truths:
    - "RVshare manual entry form shows property display names (not blank) in the property dropdown"
    - "Selecting any available category on a bank transaction succeeds without 422 error"
    - "Income categories no longer appear in the bank transaction category dropdown"
    - "app/accounting/revenue.py docstring accurately describes automatic invocation"
    - "IncomeStatementTab.tsx has zero @ts-expect-error suppressions"
  artifacts:
    - path: "frontend/src/components/actions/RVshareEntryForm.tsx"
      provides: "Property dropdown rendering with display_name"
      contains: "display_name"
    - path: "frontend/src/components/finance/CategorySelect.tsx"
      provides: "Category dropdown without income categories"
      must_not_contain: "INCOME_CATEGORIES"
    - path: "app/accounting/revenue.py"
      provides: "Accurate docstrings for revenue recognition"
      contains: "automatically as a BackgroundTask"
    - path: "frontend/src/components/reports/IncomeStatementTab.tsx"
      provides: "Clean TypeScript without suppressions"
      must_not_contain: "@ts-expect-error"
  key_links:
    - from: "frontend/src/components/actions/RVshareEntryForm.tsx"
      to: "/api/dashboard/properties"
      via: "Property interface matching API response"
      pattern: "display_name"
    - from: "frontend/src/components/finance/CategorySelect.tsx"
      to: "app/accounting/expenses.py"
      via: "Category values matching backend ALL_CATEGORIES"
      pattern: "EXPENSE_CATEGORIES.*NON_EXPENSE_CATEGORIES"
---

<objective>
Fix 2 integration bugs and 2 tech debt items identified in v1-MILESTONE-AUDIT.md.

Purpose: Close all remaining audit items so the project has zero known integration bugs and zero unnecessary code suppressions.
Output: Four surgical file edits with no behavioral regressions.
</objective>

<execution_context>
@/Users/tunderhill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tunderhill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1-MILESTONE-AUDIT.md
@.planning/phases/13-integration-bugfixes-cleanup/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix integration bugs (property dropdown + income categories)</name>
  <files>
    frontend/src/components/actions/RVshareEntryForm.tsx
    frontend/src/components/finance/CategorySelect.tsx
  </files>
  <action>
**Fix 1: RVshareEntryForm property dropdown blank labels**

In `frontend/src/components/actions/RVshareEntryForm.tsx`:

1. Change the `Property` interface (lines 17-21) from `name: string` to `display_name: string`. The backend `/api/dashboard/properties` endpoint returns `display_name`, not `name`. The interface must match the API response shape.

2. Change the SelectItem render (line 328) from `{p.name}` to `{p.display_name}`. This is the label text shown in the property dropdown.

Both changes are in the same file. No other files reference this `Property` interface.

**Fix 2: Remove income categories from CategorySelect**

In `frontend/src/components/finance/CategorySelect.tsx`:

1. Remove the `INCOME_CATEGORIES` export entirely (lines 33-38). These 4 values (`rental_income`, `cleaning_fee`, `service_fee`, `other_income`) describe booking revenue, not bank transaction categorization. The backend's `ALL_CATEGORIES` (in `app/accounting/expenses.py`) does not include them, causing 422 when selected.

2. Update the `ALL_CATEGORIES` export (line 47) from `[...EXPENSE_CATEGORIES, ...INCOME_CATEGORIES, ...NON_EXPENSE_CATEGORIES]` to `[...EXPENSE_CATEGORIES, ...NON_EXPENSE_CATEGORIES]`. Remove the `INCOME_CATEGORIES` spread.

3. Remove the Income `SelectGroup` from the JSX (the block rendering `INCOME_CATEGORIES.map(...)` inside a SelectGroup with SelectLabel "Income", approximately lines 151-158). Keep the Expenses and Other SelectGroups intact.

No other file imports `INCOME_CATEGORIES` or `ALL_CATEGORIES` -- only `TransactionRow.tsx` imports from this module, and it imports only the `CategorySelect` component.

Decision [04-01] locked `NON_EXPENSE_CATEGORIES`. The correct fix is removing income categories from the frontend, not adding them to the backend.
  </action>
  <verify>
Run `cd /Users/tunderhill/development/airbnb-tools/frontend && npx tsc --noEmit` to confirm no TypeScript errors.

Grep to confirm:
- `grep -c "display_name" frontend/src/components/actions/RVshareEntryForm.tsx` returns 2 (interface + render)
- `grep -c "INCOME_CATEGORIES" frontend/src/components/finance/CategorySelect.tsx` returns 0
- `grep -c "p.name" frontend/src/components/actions/RVshareEntryForm.tsx` returns 0
  </verify>
  <done>
RVshareEntryForm Property interface uses display_name and renders {p.display_name} in the dropdown. CategorySelect no longer exports INCOME_CATEGORIES, ALL_CATEGORIES contains only EXPENSE + NON_EXPENSE, and the Income SelectGroup is removed from the dropdown JSX.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix tech debt (stale docstring + @ts-expect-error suppressions)</name>
  <files>
    app/accounting/revenue.py
    frontend/src/components/reports/IncomeStatementTab.tsx
  </files>
  <action>
**Fix 3: Update revenue.py docstrings**

In `app/accounting/revenue.py`:

1. Update the module-level docstring (lines 1-5). Replace the first paragraph:

FROM:
```
Revenue recognition is OPERATOR-TRIGGERED via API — it is NOT called automatically
on booking import. The operator reviews imported bookings and then explicitly triggers
recognition (batch or per-booking) through the API.
```

TO:
```
Revenue recognition runs automatically as a BackgroundTask after each CSV import
(wired by Phase 9). It can also be triggered explicitly via the API operator endpoints
(POST /api/accounting/revenue/recognize and /recognize-all).
```

Keep the rest of the module docstring (Platforms, Fee model sections) unchanged.

2. Update the `recognize_booking_revenue()` function docstring (lines 152-153). Replace:

FROM:
```
OPERATOR-TRIGGERED. Must be called explicitly from an API endpoint — this
function is never invoked automatically during CSV import.
```

TO:
```
Called automatically as a BackgroundTask after CSV import, and also available
via the operator-triggered API endpoints. Safe to call multiple times (idempotent).
```

Keep the rest of the function docstring (For Airbnb, For VRBO/RVshare sections) unchanged.

**Fix 4: Remove @ts-expect-error suppressions from IncomeStatementTab**

In `frontend/src/components/reports/IncomeStatementTab.tsx`:

1. Remove the comment line at line 289: `// @ts-expect-error isSubtotal not in base type but MonthlyTable handles it`

2. Remove the comment line at line 313: `// @ts-expect-error isSubtotal not in base type but MonthlyTable handles it`

These suppressions are unnecessary because `MonthlyTableRow` in `MonthlyTable.tsx` already declares `isSubtotal?: boolean` (line 10). The `@ts-expect-error` comments would actually cause a TypeScript error themselves if the type WERE missing, since they suppress a non-existent error. Remove ONLY the comment lines; leave the `isSubtotal: true` property assignments intact.
  </action>
  <verify>
Run `cd /Users/tunderhill/development/airbnb-tools/frontend && npx tsc --noEmit` to confirm no TypeScript errors.

Grep to confirm:
- `grep -c "OPERATOR-TRIGGERED" app/accounting/revenue.py` returns 0
- `grep -c "BackgroundTask" app/accounting/revenue.py` returns at least 2 (module docstring + function docstring)
- `grep -c "@ts-expect-error" frontend/src/components/reports/IncomeStatementTab.tsx` returns 0

Run `cd /Users/tunderhill/development/airbnb-tools/frontend && npx vite build` to confirm the frontend builds cleanly.
  </verify>
  <done>
revenue.py module-level and function-level docstrings accurately describe automatic BackgroundTask invocation. IncomeStatementTab.tsx has zero @ts-expect-error comments. TypeScript compiles and frontend builds cleanly.
  </done>
</task>

</tasks>

<verification>
All four success criteria from the roadmap must be verified:

1. `grep "display_name" frontend/src/components/actions/RVshareEntryForm.tsx` shows both interface field and render usage
2. `grep "INCOME_CATEGORIES" frontend/src/components/finance/CategorySelect.tsx` returns no matches
3. `grep "BackgroundTask" app/accounting/revenue.py` shows updated docstring language
4. `grep "@ts-expect-error" frontend/src/components/reports/IncomeStatementTab.tsx` returns no matches
5. `cd frontend && npx tsc --noEmit` passes with zero errors
6. `cd frontend && npx vite build` succeeds
</verification>

<success_criteria>
- RVshare property dropdown renders display_name (not blank)
- Bank transaction category dropdown shows only Expenses and Other groups (no Income group)
- revenue.py docstrings say "BackgroundTask" not "OPERATOR-TRIGGERED"
- IncomeStatementTab.tsx has zero @ts-expect-error lines
- TypeScript compilation and Vite build both pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-integration-bugfixes-cleanup/13-01-SUMMARY.md`
</output>
