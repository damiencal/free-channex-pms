---
phase: 15-project-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - docker-compose.yml
  - .env.example
  - app/main.py
  - app/logging.py
  - manage.py
  - README.md
autonomous: true

must_haves:
  truths:
    - "pyproject.toml declares name = 'roost-rental' and description references Roost"
    - "docker-compose.yml has services roost-db and roost-api with image: roost and name: roost at top level"
    - ".env.example DATABASE_URL uses @roost-db: as hostname"
    - "app/main.py startup log says 'Starting Roost' and FastAPI title is 'Roost'"
    - "manage.py CLI help says 'Roost CLI' and setup banner says 'Roost'"
    - "README.md heading is 'Roost' and references docker compose restart roost-api"
    - "uv.lock contains name = 'roost-rental' (regenerated via uv sync)"
  artifacts:
    - path: "pyproject.toml"
      contains: 'name = "roost-rental"'
    - path: "docker-compose.yml"
      contains: "roost-api"
    - path: ".env.example"
      contains: "@roost-db:"
    - path: "app/main.py"
      contains: 'title="Roost"'
    - path: "manage.py"
      contains: "Roost CLI"
    - path: "README.md"
      contains: "# Roost"
  key_links:
    - from: "docker-compose.yml"
      to: ".env.example"
      via: "DATABASE_URL hostname matches service name roost-db"
      pattern: "@roost-db:"
    - from: "docker-compose.yml"
      to: "docker-compose.yml"
      via: "depends_on references roost-db (not db)"
      pattern: "depends_on.*roost-db"
---

<objective>
Rename Python package, Docker Compose services, and backend identity strings from "rental-management" / "Rental Management Suite" to "roost-rental" / "Roost".

Purpose: Establishes the new "Roost" identity across the Python distribution, Docker infrastructure, and backend application strings. This is the foundation — Docker service names cascade into DATABASE_URL hostnames, so this must be done atomically.

Output: Updated pyproject.toml, docker-compose.yml, .env.example, app/main.py, app/logging.py, manage.py, README.md, and regenerated uv.lock — all reflecting the Roost identity.
</objective>

<execution_context>
@/Users/tunderhill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tunderhill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-project-rename/15-CONTEXT.md
@.planning/phases/15-project-rename/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Python package and Docker Compose services</name>
  <files>pyproject.toml, docker-compose.yml, .env.example</files>
  <action>
**pyproject.toml:**
- Change `name = "rental-management"` to `name = "roost-rental"`
- Change `description = "Self-hosted vacation rental management platform"` to `description = "Roost — Self-hosted vacation rental operations platform"`
- Leave everything else unchanged: `packages = ["app"]`, dependencies, scripts, build-system

**docker-compose.yml** — replace the ENTIRE file with this exact content:
```yaml
name: roost

services:
  roost-db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  roost-api:
    build: .
    image: roost
    restart: unless-stopped
    command: >
      sh -c "/app/.venv/bin/alembic upgrade head &&
             /app/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000"
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config:ro
      - ./templates:/app/templates:ro
      - ./pdf_mappings:/app/pdf_mappings:ro
      - ./archive:/app/archive
      - ./confirmations:/app/confirmations:ro
    depends_on:
      roost-db:
        condition: service_healthy
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  db_data:
```

Key changes: `name: roost` at top, `db` → `roost-db`, `app` → `roost-api`, added `image: roost`, `depends_on: db:` → `depends_on: roost-db:`. Volume `db_data` stays unchanged.

**.env.example:**
- Change `DATABASE_URL=postgresql+psycopg://rental:changeme@db:5432/rental_management` to `DATABASE_URL=postgresql+psycopg://rental:changeme@roost-db:5432/rental_management`
- Only the hostname changes (`@db:` → `@roost-db:`). POSTGRES_DB, POSTGRES_USER stay unchanged.

**After edits, run `uv sync`** to regenerate uv.lock with the new package name.

**IMPORTANT — Do NOT change:**
- `POSTGRES_DB=rental_management` (database name, not project identity)
- `POSTGRES_USER=rental` (database user, not project identity)
- `db_data` volume name (avoid breaking existing installs)
- `packages = ["app"]` in pyproject.toml (module dir stays app/)
  </action>
  <verify>
1. `grep 'name = "roost-rental"' pyproject.toml` returns a match
2. `grep 'roost-api' docker-compose.yml` returns a match
3. `grep 'roost-db' docker-compose.yml` returns a match
4. `grep 'image: roost' docker-compose.yml` returns a match
5. `grep '@roost-db:' .env.example` returns a match
6. `grep 'name = "roost-rental"' uv.lock` returns a match (after uv sync)
7. `grep 'rental-management' pyproject.toml uv.lock` returns NO matches
  </verify>
  <done>pyproject.toml declares roost-rental, docker-compose.yml uses roost-db/roost-api/image:roost with name:roost at top, .env.example DATABASE_URL uses @roost-db: hostname, uv.lock is regenerated with new package name</done>
</task>

<task type="auto">
  <name>Task 2: Update backend application strings and README</name>
  <files>app/main.py, app/logging.py, manage.py, README.md</files>
  <action>
**app/main.py:**
- Line 65: Change `log.info("Starting Rental Management Suite")` to `log.info("Starting Roost")`
- Lines 138-142: Change FastAPI constructor:
  - `title="Rental Management Suite"` → `title="Roost"`
  - `description="Self-hosted vacation rental management platform"` → `description="Rental Operations Platform — Self-hosted vacation rental management"`

**app/logging.py:**
- Line 2 of docstring: Change `"Structlog configuration for the Rental Management Suite."` to `"Structlog configuration for Roost."`

**manage.py:**
- Line 2 of docstring: Change `"Rental Management Suite CLI."` to `"Roost CLI."`
- Line 25: Change `typer.Typer(help="Rental Management Suite CLI")` to `typer.Typer(help="Roost CLI")`
- Line 55: Change `typer.echo("  Rental Management Suite — Property Setup")` to `typer.echo("  Roost — Property Setup")`

**README.md:**
- Line 1: Change `# Rental Management Suite` to `# Roost`
- Line 3: Change `Self-hosted vacation rental management platform. Automates booking operations from notification to accounting entry.` to `Self-hosted vacation rental operations platform. Automates booking operations from notification to accounting entry.`
- Line 59: Change `docker compose restart app` to `docker compose restart roost-api`

**IMPORTANT — Do NOT change:**
- `app/db.py` fallback DB URL `rental_management` — leave as-is (fallback, not project identity)
- `alembic/env.py` fallback DB URL — leave as-is
- `app/query/prompt.py` "vacation rental management system" — leave as-is (domain description)
- Any "Airbnb" reference that describes the booking platform
  </action>
  <verify>
1. `grep "Starting Roost" app/main.py` returns a match
2. `grep 'title="Roost"' app/main.py` returns a match
3. `grep "Roost CLI" manage.py` returns a match (should appear twice: docstring + typer)
4. `grep "Structlog configuration for Roost" app/logging.py` returns a match
5. `grep "# Roost" README.md` returns a match
6. `grep "restart roost-api" README.md` returns a match
7. `grep -r "Rental Management Suite" app/ manage.py README.md` returns NO matches
  </verify>
  <done>All backend strings and README reference "Roost" instead of "Rental Management Suite". No platform-specific "Airbnb" references were changed. No fallback DB URLs were changed.</done>
</task>

</tasks>

<verification>
Run these checks to confirm all Plan 01 objectives are met:

```bash
# Confirm no "rental-management" in package files
grep -r "rental-management" pyproject.toml uv.lock && echo "FAIL: rental-management found" || echo "PASS"

# Confirm no "Rental Management Suite" in modified files
grep -r "Rental Management Suite" app/ manage.py README.md docker-compose.yml .env.example pyproject.toml && echo "FAIL: old name found" || echo "PASS"

# Confirm new identity is present
grep 'name = "roost-rental"' pyproject.toml && echo "PASS: pyproject" || echo "FAIL"
grep 'roost-api' docker-compose.yml && echo "PASS: docker service" || echo "FAIL"
grep '@roost-db:' .env.example && echo "PASS: env hostname" || echo "FAIL"
grep 'title="Roost"' app/main.py && echo "PASS: FastAPI title" || echo "FAIL"
```
</verification>

<success_criteria>
1. pyproject.toml name is "roost-rental" and description references "Roost"
2. docker-compose.yml uses roost-db, roost-api, image: roost, name: roost
3. .env.example DATABASE_URL uses @roost-db: hostname
4. uv.lock regenerated with roost-rental (no rental-management references)
5. app/main.py, app/logging.py, manage.py all reference "Roost" not "Rental Management Suite"
6. README.md heading is "Roost" and docker command references roost-api
7. Zero matches for "Rental Management Suite" in any modified file
</success_criteria>

<output>
After completion, create `.planning/phases/15-project-rename/15-01-SUMMARY.md`
</output>
