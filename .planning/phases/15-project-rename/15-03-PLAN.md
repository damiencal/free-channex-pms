---
phase: 15-project-rename
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false

must_haves:
  truths:
    - "A project-wide search for 'airbnb-tools' in tracked files returns zero results (excluding .planning/phases/ historical artifacts and .git/)"
    - "A project-wide search for 'Rental Management Suite' in tracked files returns zero results (excluding .planning/phases/ historical artifacts)"
    - "Active .planning/ docs reference 'Roost' not 'Rental Management Suite' or 'airbnb-tools'"
    - "Frontend builds successfully (npm run build in frontend/)"
    - "Python tests pass (python -m pytest)"
    - "The local directory has been renamed from airbnb-tools to roost"
  artifacts:
    - path: ".planning/MILESTONES.md"
      contains: "Roost"
    - path: ".planning/PROJECT.md"
      contains: "Roost"
    - path: ".planning/ROADMAP.md"
      contains: "Roost"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "pyproject.toml"
      via: "RNAM-01 description matches actual package name"
      pattern: "roost-rental"
    - from: ".planning/ROADMAP.md"
      to: "docker-compose.yml"
      via: "Phase 15 success criteria reference roost service names"
      pattern: "roost"
---

<objective>
Update active planning docs to reference "Roost", run full project-wide verification that no old identity strings remain, then rename the directory from airbnb-tools to roost as the final step.

Purpose: Completes the identity transformation by updating internal docs, verifying the entire codebase is clean, and performing the directory rename. This is the final plan in Phase 15 and must run after Plans 01 and 02.

Output: Updated .planning/ active docs, verified clean codebase, directory renamed to roost, and post-rename user checklist provided.
</objective>

<execution_context>
@/Users/tunderhill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tunderhill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-project-rename/15-CONTEXT.md
@.planning/phases/15-project-rename/15-RESEARCH.md
@.planning/phases/15-project-rename/15-01-SUMMARY.md
@.planning/phases/15-project-rename/15-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update active planning docs and verify clean codebase</name>
  <files>.planning/MILESTONES.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
**Update these active planning docs only** (leave all historical artifacts in .planning/phases/ and .planning/milestones/ unchanged):

**.planning/MILESTONES.md:**
- Change heading `# Project Milestones: Rental Management Suite` to `# Project Milestones: Roost`

**.planning/PROJECT.md:**
- Find the one reference to `"airbnb-tools"` in the Active requirements list and update it to `"roost"`. The line reads: `Project renamed from "airbnb-tools" to "Roost" everywhere` — this is already correct as it describes the rename intent, but verify it doesn't have a stale reference.
- Verify all other references in the file are already "Roost" (the file heading is already "# Roost")

**.planning/REQUIREMENTS.md:**
- RNAM-01 description: update to reflect the actual distribution name `roost-rental` (not just `roost`)
- RNAM-02 description: update service names to match actual (roost-api, roost-db) — current says (roost-app, roost-db) but the CONTEXT decision says roost-api
- Any other RNAM description referencing old names should be updated to match the actual implementation

**.planning/ROADMAP.md:**
- Search for any remaining `"airbnb-tools"` or `"Rental Management Suite"` references and update to `"Roost"` / `"roost"`
- Note: Some references describe the rename goal (e.g., "Rename everything from 'airbnb-tools' to 'Roost'") — these are correct as-is because they describe what was done. Only update references that USE the old name AS the current name.

**After doc updates, run a comprehensive project-wide search:**

```bash
# Search all tracked files for old identity strings
git ls-files | xargs grep -l "airbnb-tools" 2>/dev/null | grep -v "^\.planning/phases/" | grep -v "^\.planning/milestones/"
git ls-files | xargs grep -l "Rental Management Suite" 2>/dev/null | grep -v "^\.planning/phases/" | grep -v "^\.planning/milestones/"
```

If any results appear (excluding historical artifacts in .planning/phases/ and .planning/milestones/), fix them before proceeding. These are files missed in Plans 01 and 02.

Also run the frontend build to verify nothing is broken:

```bash
cd frontend && npm run build && cd ..
```

And run Python tests:

```bash
python -m pytest
```

If any test or build fails, diagnose and fix before proceeding.
  </action>
  <verify>
1. `grep "Rental Management Suite" .planning/MILESTONES.md` returns NO match
2. `grep "Roost" .planning/MILESTONES.md | head -1` returns a match
3. `git ls-files | xargs grep -l "airbnb-tools" 2>/dev/null | grep -v "^\.planning/phases/" | grep -v "^\.planning/milestones/"` returns empty
4. `git ls-files | xargs grep -l "Rental Management Suite" 2>/dev/null | grep -v "^\.planning/phases/" | grep -v "^\.planning/milestones/"` returns empty
5. Frontend build succeeds (`cd frontend && npm run build`)
6. Python tests pass (`python -m pytest`)
  </verify>
  <done>All active planning docs reference "Roost". A project-wide search for "airbnb-tools" and "Rental Management Suite" returns zero results in non-historical tracked files. Frontend builds and Python tests pass.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Rename directory from airbnb-tools to roost</name>
  <action>
The user must perform the directory rename because:
1. Claude's working directory IS airbnb-tools — renaming it from inside would break the shell session
2. The user needs to update their IDE workspace, terminal sessions, and any shell aliases

**Instructions for the user:**

Run these commands from the PARENT directory (e.g., `~/development/`):

```bash
cd ~/development
mv airbnb-tools roost
cd roost
```

Then verify:
```bash
# Confirm you're in the right directory
pwd  # Should show .../roost
git status  # Should show clean working tree (no changes)
ls docker-compose.yml  # Should exist
```

**Post-rename checklist:**
1. Update IDE workspace/project root to point to `roost/`
2. Update any shell aliases (e.g., `alias art='cd ~/development/airbnb-tools'` → `alias art='cd ~/development/roost'`)
3. Update any terminal window presets or shell scripts referencing the old path
4. If using an existing `.env` (not .env.example), update `DATABASE_URL` to use `@roost-db:` as the hostname
5. Run `docker compose down` (from old config) then `docker compose up --build` (from new config) to apply service renames
6. Dark mode and property selection will reset in the browser (localStorage keys changed) — just re-select
  </action>
  <resume-signal>Type "done" after renaming the directory and confirming the checklist</resume-signal>
</task>

</tasks>

<verification>
Final verification after directory rename:

```bash
# Confirm directory name
basename "$(pwd)"  # Should be "roost"

# Confirm no old identity in tracked files (excluding historical docs)
git ls-files | xargs grep -l "airbnb-tools" 2>/dev/null | grep -v "^\.planning/phases/" | grep -v "^\.planning/milestones/"
# Should return empty

git ls-files | xargs grep -l "Rental Management Suite" 2>/dev/null | grep -v "^\.planning/phases/" | grep -v "^\.planning/milestones/"
# Should return empty

# Confirm new identity
grep 'name = "roost-rental"' pyproject.toml
grep 'roost-api' docker-compose.yml
grep '"name": "roost"' frontend/package.json
grep "Roost" .planning/MILESTONES.md
```
</verification>

<success_criteria>
1. All active .planning/ docs reference "Roost" (not "Rental Management Suite" or "airbnb-tools")
2. Project-wide search for "airbnb-tools" and "Rental Management Suite" returns zero results in non-historical tracked files
3. Frontend build passes
4. Python tests pass
5. Directory is renamed from airbnb-tools to roost
6. User has been provided with post-rename checklist
</success_criteria>

<output>
After completion, create `.planning/phases/15-project-rename/15-03-SUMMARY.md`
</output>
