---
phase: 07-dashboard
plan: 05
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - frontend/src/components/actions/ActionsList.tsx
  - frontend/src/components/actions/ActionItem.tsx
  - frontend/src/components/actions/ActionsTab.tsx
  - frontend/src/components/reports/ReportsTab.tsx
autonomous: true

must_haves:
  truths:
    - "Actions tab shows a flat list of pending items sorted by urgency"
    - "Each action item has a type icon/color indicating resort form, VRBO message, or unreconciled transaction"
    - "Clicking an action item expands it to show details and an action button"
    - "Empty state shows 'No pending actions' text"
    - "Reports tab shows a placeholder with links to existing report endpoints"
  artifacts:
    - path: "frontend/src/components/actions/ActionsTab.tsx"
      provides: "Actions tab container with data fetching"
      min_lines: 20
    - path: "frontend/src/components/actions/ActionsList.tsx"
      provides: "Sorted list of action items"
      min_lines: 40
    - path: "frontend/src/components/actions/ActionItem.tsx"
      provides: "Expandable action item with details and action button"
      min_lines: 40
    - path: "frontend/src/components/reports/ReportsTab.tsx"
      provides: "Reports placeholder tab"
      min_lines: 10
  key_links:
    - from: "frontend/src/components/actions/ActionsTab.tsx"
      to: "/api/dashboard/actions"
      via: "useActions hook"
      pattern: "useActions"
    - from: "frontend/src/components/actions/ActionItem.tsx"
      to: "/compliance/submit"
      via: "action button POST"
      pattern: "compliance|communication"
---

<objective>
Build the Actions tab showing pending items (resort forms, VRBO messages, unreconciled transactions) as an expandable list sorted by urgency, and a Reports tab placeholder.

Purpose: The Actions tab is Kim's task queue. She sees what needs attention and can act on it without navigating to different system areas. The Reports tab is a placeholder for Phase 8 or future work.
Output: A functional Actions tab with expandable items and action buttons, plus a Reports tab placeholder.
</objective>

<execution_context>
@/Users/tunderhill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tunderhill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard/07-RESEARCH.md
@.planning/phases/07-dashboard/07-CONTEXT.md
@.planning/phases/07-dashboard/07-01-SUMMARY.md
@.planning/phases/07-dashboard/07-02-SUMMARY.md
@frontend/src/api/client.ts
@frontend/src/hooks/useActions.ts
@frontend/src/store/usePropertyStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actions tab with expandable action items</name>
  <files>
    frontend/src/components/actions/ActionsTab.tsx
    frontend/src/components/actions/ActionsList.tsx
    frontend/src/components/actions/ActionItem.tsx
  </files>
  <action>
    **ActionsTab** (frontend/src/components/actions/ActionsTab.tsx):
    - Fetches actions using useActions() hook (from Plan 03's hooks, or create here if Plan 03 hasn't run)
    - Loading state: show skeleton list (3-4 skeleton rows)
    - Error state: show ErrorAlert with retry
    - Empty state: simple "No pending actions" text in muted color, centered. No illustrations or fanfare (per CONTEXT.md)
    - Passes action items to ActionsList

    **ActionsList** (frontend/src/components/actions/ActionsList.tsx):
    - Receives sorted action items array (already sorted by urgency from backend)
    - Uses shadcn Accordion (or Collapsible) to render expandable items
    - Only one item can be expanded at a time (accordion mode="single")
    - Each item renders an ActionItem component

    **ActionItem** (frontend/src/components/actions/ActionItem.tsx):
    - Collapsed state shows:
      - Type icon using lucide-react icons:
        - resort_form: FileText icon in amber/orange
        - vrbo_message: MessageSquare icon in blue
        - unreconciled: DollarSign icon in gray
      - Guest name (bold)
      - Brief description based on type:
        - resort_form: "Resort form due in {N} days" or "Resort form overdue" (if negative days)
        - vrbo_message: "{message_type} message ready to send"
        - unreconciled: "{platform} booking - ${amount} unreconciled"
      - Property slug as a subtle tag/badge on the right

    - Expanded state shows additional details:
      - resort_form: check-in date, property name, "Submit" button that POSTs to /compliance/submit/{booking_id}
      - vrbo_message: message type, scheduled date, property name, "Mark as Sent" button that POSTs to /communication/confirm/{log_id}
      - unreconciled: platform, check-in date, amount, property name, "View in Reconciliation" text (no action button — reconciliation is done via CSV upload, not dashboard)
    - Action buttons use shadcn Button component (default variant)
    - After action button click: show loading spinner on button, then refetch actions list on success, show error toast on failure
    - Use TanStack Query's `useMutation` for action button clicks with `onSuccess: () => queryClient.invalidateQueries({ queryKey: ['actions'] })`
    - Toast notifications: use a simple approach — either the shadcn Alert inline or a temporary success message. Do NOT add a new toast library; keep it simple.
  </action>
  <verify>
    cd frontend && npm run build — no errors.
    cd frontend && npx tsc --noEmit — no type errors.
  </verify>
  <done>
    Actions tab renders a sorted list of pending items with type icons, expandable details, and action buttons (Submit for resort forms, Mark as Sent for messages). Empty state shows "No pending actions." Action buttons trigger API calls and refetch the list on success.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reports tab placeholder</name>
  <files>
    frontend/src/components/reports/ReportsTab.tsx
  </files>
  <action>
    **ReportsTab** (frontend/src/components/reports/ReportsTab.tsx):
    - Simple placeholder content:
      - Title: "Reports"
      - Three shadcn Cards in a vertical stack, each linking to an existing API endpoint:
        1. "Profit & Loss" — description: "Revenue by platform, expenses by category" — note: "Available via API at /api/reports/pl"
        2. "Balance Sheet" — description: "Assets, liabilities, and equity snapshot" — note: "Available via API at /api/reports/balance-sheet"
        3. "Income Statement" — description: "Revenue and expense breakdown" — note: "Available via API at /api/reports/income-statement"
      - Each card shows the report description. Cards are not clickable (no UI for rendering these reports yet — they're API-only from Phase 4).
      - Subtle muted text at bottom: "Full report views coming soon"
    - This is intentionally minimal. CONTEXT.md lists Reports as a tab but specifies no content. The roadmap plans don't include report rendering UI. This placeholder acknowledges the tab exists without over-building.
  </action>
  <verify>
    cd frontend && npm run build — no errors.
    cd frontend && npx tsc --noEmit — no type errors.
  </verify>
  <done>
    Reports tab shows 3 cards describing available report types with a "coming soon" note. No interactive functionality — serves as a placeholder for future development.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` — no errors
2. `cd frontend && npx tsc --noEmit` — no type errors
3. Actions tab shows sorted pending items when data exists
4. Expanding an item reveals details and action button
5. Action buttons call correct API endpoints
6. Empty state renders correctly when no actions
7. Reports tab shows placeholder cards
</verification>

<success_criteria>
- Actions tab displays flat list sorted by urgency (resort forms first, then messages, then unreconciled)
- Each item shows type icon/color, guest name, brief description, property badge
- Expanding an item shows details and action button
- "Submit" button for resort forms calls compliance API
- "Mark as Sent" for messages calls communication API
- Successful action refetches the list
- Empty state: "No pending actions" in muted text
- Reports tab: 3 placeholder cards describing available reports
- Property selector filters actions list
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard/07-05-SUMMARY.md`
</output>
