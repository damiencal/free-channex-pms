---
phase: 07-dashboard
plan: 06
type: execute
wave: 3
depends_on: ["07-03", "07-04", "07-05"]
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/Header.tsx
  - frontend/src/index.css
  - frontend/src/components/shared/EmptyState.tsx
autonomous: false

must_haves:
  truths:
    - "Actions tab badge shows correct count and updates when actions are dismissed"
    - "All tabs show appropriate empty states when no data exists"
    - "All tabs show loading skeletons while data is fetching"
    - "All tabs show error alerts with retry when API calls fail"
    - "Dashboard is readable on mobile (single-column layout, no horizontal overflow)"
    - "Dark mode applies consistently across all tabs, charts, and components"
    - "Kim can navigate the dashboard and understand what she sees without help"
  artifacts:
    - path: "frontend/src/components/shared/EmptyState.tsx"
      provides: "Reusable empty state component"
      min_lines: 10
    - path: "frontend/src/index.css"
      provides: "Responsive and dark mode CSS refinements"
  key_links:
    - from: "frontend/src/components/layout/AppShell.tsx"
      to: "frontend/src/hooks/useActions.ts"
      via: "actions count for tab badge"
      pattern: "useActions|total_count|badge"
---

<objective>
Polish the dashboard with consistent empty/error/loading states, mobile responsiveness, dark mode audit, action badge live updates, and human verification of the complete UI.

Purpose: This is the final integration pass. Individual tabs work, but the whole must feel cohesive. Kim is the primary user -- the interface must be intuitive without explanation.
Output: A polished, production-ready dashboard that passes visual and functional human verification.
</objective>

<execution_context>
@/Users/tunderhill/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tunderhill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard/07-RESEARCH.md
@.planning/phases/07-dashboard/07-CONTEXT.md
@.planning/phases/07-dashboard/07-03-SUMMARY.md
@.planning/phases/07-dashboard/07-04-SUMMARY.md
@.planning/phases/07-dashboard/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish integration — badges, empty states, mobile, dark mode</name>
  <files>
    frontend/src/components/layout/AppShell.tsx
    frontend/src/components/layout/Header.tsx
    frontend/src/components/shared/EmptyState.tsx
    frontend/src/index.css
  </files>
  <action>
    **Actions badge on tab** (AppShell.tsx):
    - The Actions tab label should show a shadcn Badge with the total_count from useActions()
    - Badge style: destructive variant when count > 0 (red), hidden when count = 0
    - Format: "Actions 3" or just the number in a badge next to "Actions"
    - Badge updates automatically when TanStack Query refetches actions data (e.g., after dismissing an action)

    **EmptyState component** (frontend/src/components/shared/EmptyState.tsx):
    - Reusable: accepts title (string), description (string, optional)
    - Renders centered text in muted color
    - No illustrations or icons — just clean text (per CONTEXT.md: "no illustrations or fanfare")
    - Use in:
      - Home tab: if no metrics data (unlikely but handle gracefully)
      - Calendar: "No bookings for this month" when month has no bookings
      - Actions: "No pending actions" (may already exist from Plan 05 — ensure consistent component usage)

    **Mobile responsiveness audit** (index.css + component updates):
    - Verify all grid layouts collapse to single column on mobile (sm breakpoint)
    - Header: property selector and dark mode toggle should be accessible on mobile (no overflow)
    - Tabs: if 4 tabs don't fit, ensure they scroll horizontally (not wrap awkwardly)
    - Stat cards: stack vertically on mobile
    - Charts: full width on mobile, maintain readable size
    - Calendar month view: ensure day cells don't become too small; booking text may truncate
    - Timeline view: horizontal scroll is expected on mobile (already handled)
    - Action items: full width, padding appropriate for touch targets

    **Dark mode consistency audit**:
    - Verify all shadcn components respect the dark class (they should by default)
    - Chart colors: ensure platform colors have dark mode variants (set in platformColors.ts CSS vars)
    - Calendar booking bars: verify contrast in dark mode
    - Stat card YoY arrows: green/red colors should be readable in both modes
    - Header background: distinct from content area in both modes
    - Focus rings and hover states visible in dark mode

    **Minor polish**:
    - Add `<title>Rental Dashboard</title>` to index.html if not already present
    - Ensure favicon exists (use Vite default or a simple SVG)
    - Add a subtle border or shadow between header and tab content for visual separation
    - Ensure all loading skeletons match the dimensions of their loaded content (no layout shift)
  </action>
  <verify>
    cd frontend && npm run build — no errors.
    cd frontend && npx tsc --noEmit — no type errors.
  </verify>
  <done>
    Actions tab badge shows live count. EmptyState component used consistently across tabs. Mobile layout verified (single column, no overflow). Dark mode verified (all components, charts, booking bars). Minor polish applied (title, visual separation, skeleton sizing).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete dashboard with 4 tabs:
    - **Home**: Financial stat cards (YTD Revenue, Expenses, Profit) with YoY comparison, booking trend bar chart, occupancy donut chart, actions preview
    - **Calendar**: Month grid view with booking bars (colored by platform), timeline/Gantt view, booking detail popovers, month navigation
    - **Reports**: Placeholder with report type cards
    - **Actions**: Pending items list with expand/action functionality
    Plus: global property selector, dark mode toggle, responsive mobile layout
  </what-built>
  <how-to-verify>
    1. Start the application:
       - For full Docker test: `docker-compose up --build` then visit http://localhost:8000
       - For dev test: In one terminal run `cd frontend && npm run dev` (Vite at :5173), in another run the FastAPI backend

    2. **Home tab check**:
       - Verify 3 stat cards show at top (may show $0 if no data yet — that's OK)
       - Verify charts render (may be empty without data — skeleton/empty states should show)
       - Verify actions preview card appears at bottom

    3. **Calendar tab check**:
       - Click "Calendar" tab
       - Verify month grid renders with correct days
       - If bookings exist: verify colored bars span correct dates
       - Click a booking bar: verify popover shows details
       - Click prev/next arrows: month changes
       - Toggle to "Timeline" view: verify property rows appear
       - Toggle back to "Month" view

    4. **Actions tab check**:
       - Click "Actions" tab
       - If pending items exist: verify they appear sorted
       - Click an item to expand: verify details and action button appear
       - If no items: verify "No pending actions" empty state

    5. **Reports tab check**:
       - Click "Reports" tab
       - Verify 3 placeholder cards visible

    6. **Property selector check**:
       - Change property in header dropdown
       - Verify data updates across all tabs

    7. **Dark mode check**:
       - Click the sun/moon toggle
       - Verify all elements switch to dark theme
       - Verify charts and calendar bars are visible in dark mode

    8. **Mobile check** (resize browser window to ~375px width):
       - Verify no horizontal overflow
       - Verify tabs are accessible
       - Verify content stacks vertically
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 7, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` — no errors
2. `cd frontend && npx tsc --noEmit` — no type errors
3. `docker build .` — multi-stage build succeeds
4. Human verification of complete dashboard UI
</verification>

<success_criteria>
- Dashboard home shows financial metrics at a glance without navigation
- Calendar shows bookings color-coded by platform in month and timeline views
- Pending actions are visible and actionable
- Kim can check financial and booking status without explanation
- All 4 tabs render correctly in light and dark mode
- Mobile layout is usable (single column, no overflow)
- Property selector filters all tabs
- Loading, error, and empty states handled consistently
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard/07-06-SUMMARY.md`
</output>
