"""Channex.io PMS integration package.

Provides:
  - HTTP client with rate limiting and automatic retry
  - Custom exception hierarchy
  - Service modules for properties, calendar, reservations, messaging, and reviews
  - Webhook receiver and background sync jobs
"""
